# Word 截图最终可用清单

这份清单只保留“整屏 / 整个 Word 窗口”版本，作为正式可用图。

## 正式版

1. `03-section-breaks-menu-full.jpg`
   用途：`布局 -> 分隔符 / 分节符` 菜单。
2. `08-cross-reference-dialog-full.jpg`
   用途：`引用 -> 交叉引用` 对话框。
3. `10-mail-merge-select-recipients-full.jpg`
   用途：`邮件 -> 选择收件人` 菜单。
4. `13-header-footer-edit-mode-full.jpg`
   用途：页眉页脚编辑态，含 `页眉和页脚` 上下文工具。
5. `14-page-number-menu-full.jpg`
   用途：`插入 -> 页码` 菜单。
6. `15-page-number-format-dialog-full.jpg`
   用途：`插入 -> 页码 -> 设置页码格式` 对话框。

## 可继续沿用但不是“full”命名的整屏版

1. `05-insert-tab.jpg`
   用途：插入选项卡总入口。
2. `07-references-tab-toc-crossref.jpg`
   用途：引用选项卡总入口。
3. `09-mailings-tab.jpg`
   用途：邮件选项卡总入口。
4. `12-print-preview.jpg`
   用途：打印预览界面。

## 备用 / 非正式版

以下文件暂时不作为最终交付清单，只保留作参考：

1. 所有非 `-full` 的局部菜单图或对话框图。
2. 所有 `tmp-*.jpg` 过程图。
3. `13-header-footer-edit-mode.jpg`
4. `14-header-footer-edit-mode.jpg`
5. `03-breaks-menu-section-breaks.jpg`

## 使用建议

1. 以后插图时，优先从“正式版”里取图。
2. 如果要继续补图，文件名延续 `*-full.jpg` 规则。
3. 如果后面要做最终交付包，建议只导出这份清单里的“正式版”和“可继续沿用”两组。
